/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://kitahack2026.my/', // domain
  generateRobotsTxt: true, // Generate robots.txt automatically
  changefreq: 'weekly', // Optional: page change frequency
  priority: 0.7, // Optional: page priority
  sitemapSize: 5000, // Optional: max URLs per sitemap file
};
